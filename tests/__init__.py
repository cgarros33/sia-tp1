"""Suite de regresión del TP.

Existe como paquete —con `__init__.py`— para que pytest agregue la raíz del
repositorio a `sys.path` y `import src...`, `import main` e
`import verificaciones...` funcionen sin instalar nada ni tocar PYTHONPATH.
"""
